sudo: required
services:
    - docker
    
before_install:
    - docker build -t wizard1988/docker-react -f Dockerfile.dev .
    
script:
    - docker run wizard1988/docker-react npm run test -- --coverage